node_modules/
.driftcheck/

# Internal planning docs — Atlas-specific/private narrative, not meant
# for this repo's public history. Kept locally, not tracked. Moved out
# of root entirely (not just gitignored) so driftcheck spec's own
# filename scanning never sees them — they matched its *_spec_v0_NN.md
# pattern by coincidence (unpadded "v0_1"), which made driftcheck spec
# treat this hand-written doc as if it were an existing generated spec.
planning/
