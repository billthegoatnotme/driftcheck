# Search ideas (mid-work)

A plan started during the current cycle. It stays here until the
next `driftcheck spec close`, which lists it in the new handoff and
moves it to `previous/example-app_plan_previous/`.

- Full-text search over local notes
- Match on tags first, then body
